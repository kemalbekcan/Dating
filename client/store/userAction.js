import { ADD_USER } from './taskTypes';
import axios from 'axios';

export const addUser = (user) => async (dispatch) => {
  console.log('userdsadasda');
  //   type: ADD_USER,
  //   payload: user,
};
